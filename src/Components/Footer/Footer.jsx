import style from './Footer.module.scss';

const Footer = () => {
  return (
    <footer className={style.footer}>
      <div className={style.footerText}>footer</div>
    </footer>
  );
};

export default Footer;
